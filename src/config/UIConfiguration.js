/**
 * UI Configuration
 * Centralized configuration for UI features and behaviors
 */

export const UIConfiguration = {
  /**
   * Enable/disable sidebar minimize/collapse functionality
   * When false, sidebar will always be expanded
   * When true, sidebar can be collapsed/expanded via hover or toggle
   */
  minimizeSidebar: false,
};

